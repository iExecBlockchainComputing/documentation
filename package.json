{
  "name": "documentation",
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.1.2",
    "prettier": "^2.8.3"
  },
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{md,json,yaml}": "prettier --write"
  }
}
